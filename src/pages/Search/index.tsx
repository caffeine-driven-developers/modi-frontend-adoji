import React from 'react';
import styled from 'styled-components';

const Search: React.FC = () => {
  return <Wrapper>This is search template!</Wrapper>;
};

const Wrapper = styled.div``;

export default Search;
