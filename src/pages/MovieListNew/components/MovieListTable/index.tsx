import React from 'react';
import styled from 'styled-components';

const MovieListTable: React.FC = () => {
  return <Wrapper>MovieListTable template!</Wrapper>;
};

const Wrapper = styled.div``;

export default MovieListTable;
