import React from 'react';
import styled from 'styled-components';

const MovieSearchForm: React.FC = () => {
  return <Wrapper>MovieSearchForm template!</Wrapper>;
};

const Wrapper = styled.div``;

export default MovieSearchForm;
